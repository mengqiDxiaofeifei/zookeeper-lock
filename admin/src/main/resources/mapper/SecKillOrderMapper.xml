<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sparksys.mall.admin.dao.SecKillOrderDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.sparksys.mall.admin.entity.SecKillOrder">
        <id column="id" property="id"/>
        <result column="sec_kill_id" property="secKillId"/>
        <result column="user_id" property="userId"/>
        <result column="state" property="state"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, sec_kill_id, user_id, state, create_time
    </sql>

</mapper>
